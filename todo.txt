- Name this.

	- AutoCache (not taken)
	- MemoStore

- Add etags.

- Decide if we should store the current time as well, so that something may discarded if the requested maxage has changed.

	cache.get('key', func, maxage=3600)
	cache.get('key', func, maxage=60) # This will recalculate

- The data tuple will be:
	(creation, expiry, etag, value)